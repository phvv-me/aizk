# Demo edit map

Both assemblies are silent, 1920 by 1080 and 30 frames per second. Keep every raw clip unchanged
and edit from copies or the derived terminal clip.

## Complete walkthrough

The complete walkthrough is about nine minutes and ten seconds. It is the primary source for
the narrated submission edit.

| Range | Picture | Evidence |
| --- | --- | --- |
| 0 to 18 seconds | Deployed landing page | Product framing and one-action onboarding |
| 18 to 38 seconds | Public setup guide | Machine-readable setup discovery |
| 38 to 70 seconds | Real Codex Luna setup | Agent-created skill, MCP configuration and project guidance |
| 70 to 86 seconds | Application and Logto handoff | Account creation or sign-in entry point |
| 86 to 233 seconds | Live OpenCode workflow | Maya identity, private Atlas write, queue drain and grounded recall |
| 233 to 310 seconds | Two grounded questions | Different wording resolved to the same source and privacy receipt |
| 310 to 415 seconds | Authenticated console | Processing, sources, findings, subjects and themes |
| 415 to 470 seconds | AWS architecture | Minimal deployment, routes, limits and reproduction path |
| 470 to 549 seconds | Cloud evidence | Public health, fresh ccloud state, C-SPANN and Lambda measurements |

The main workflow source continues beyond the assembled cut with two unsuccessful queries against
the older six-note corpus. Maya cannot see those notes under her current scope. The raw footage is
retained for diagnosis and excluded from the complete walkthrough.

## Short rough cut

`rough-cut.mp4` is about 78 seconds and covers landing, setup discovery, agent configuration and
the Logto handoff. It is useful when only the onboarding story is needed.

## Available source footage

| Clip | Approximate duration | Notes |
| --- | --- | --- |
| `raw/01-homepage.mp4` | 18 seconds | Slow browser movement with the one-action panel visible |
| `raw/02-agent-guide.mp4` | 20 seconds | Readable machine-facing setup instructions |
| `raw/03-codex-setup.mp4` | 100 seconds | Complete successful run with standard Codex startup metadata |
| `derived/03-codex-setup-fast.mp4` | 41 seconds | Clean opening and accelerated work without the startup block |
| `raw/04-account-flow.mp4` | 16 seconds | Application page followed by an empty Logto sign-in form |

## Remaining edit work

The evidence scenes from `../VIDEO_SCRIPT.md` now exist. The submission video still needs an
editorial pass.

1. Record the approved narration.
2. Cut the complete walkthrough to two minutes and forty-five seconds.
3. Add checked English captions and the public repository closing card.
4. Export, upload and verify playback while signed out.

## Safety review

The assembled complete walkthrough shows no token, password, email address, AWS account number,
database connection string or private user data. Disposable Codex session metadata and fictional
organization member identifiers are trimmed out. The OAuth client identifier and deployment URLs
shown are public configuration already served by the website.
