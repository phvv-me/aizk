# Devpost media

Run `bash hackathon/media/build.sh` from the repository root after changing a source SVG. Every
gallery image uses a 3 to 2 frame and remains below the Devpost size limit.

## Upload set

| File | Devpost caption |
| --- | --- |
| `thumbnail-devpost.png` | AIZK gives agents durable memory with source evidence and access boundaries. |
| `01-live-landing.jpg` | One landing page provides direct setup for Claude Code and Codex. |
| `02-product-overview.jpg` | The dashboard makes sources, processing, and connected knowledge inspectable. |
| `03-scoped-sharing.jpg` | Scope intersections and temporal correction preserve access boundaries and history. |
| `10-cockroachdb-cspann.jpg` | The redacted CockroachDB plan selects the scoped C-SPANN vector index. |
| `11-aws-operations.jpg` | Redacted AWS evidence shows the Lambda deployment and operational health. |
| `00-architecture.png` | CockroachDB keeps memory state while AWS runs the public MCP and private worker. |

Use `thumbnail-devpost.png` as the project thumbnail. Use `00-architecture.png` for the optional
architecture upload. The remaining images form the gallery in the order shown above.

Review every exported image once more before upload. It must not expose credentials, email
addresses, account identifiers, connection strings, private notes, or unrelated deployments.
